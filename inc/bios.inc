; ============================================================
; Module: bios.inc
; Purpose: BIOS jump-table offsets and any direct-BIOS macros
;          (used sparingly — most I/O routes via BDOS). Populated
;          in Story 1.4.
;
; Public:
;   (none yet — populated in Story 1.4)
;
; State owned (read/write):
;   (none)
;
; Register conventions (across public entry points):
;   (n/a)
;
; Dependencies:
;   inc/equates.inc  (will be required once content lands)
; ============================================================
