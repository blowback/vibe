# Deferred Work

Work items raised during reviews that are real but not actionable in the originating story.

## Deferred from: code review of story-1.1 (2026-05-08)

- **`make clean` doesn't recurse into `test/build/`** — `.gitignore` already reserves `test/build/`; once Story 1.6 lands the iz-cpm harness with build artifacts, top-level `make clean` will leave them stale. Address as part of Story 1.6 by adding `$(MAKE) -C test clean` to the top-level `clean` recipe and a matching `clean:` target in `test/Makefile`. (Sources: Acceptance Auditor + Blind Hunter.)

## Deferred from: code review of story-1.2 (2026-05-09)

- **`VT52_GOTO` row/col clamp must land in render path.** Out-of-range coordinates (row ≥ `SCREEN_ROWS`, col ≥ `SCREEN_COLS`) emit garbage bytes after the bias and corrupt the VT52 state machine. Story 1.11 (render pipeline) must clamp `row ∈ [0, SCREEN_ROWS-1]`, `col ∈ [0, SCREEN_COLS-1]` before composing `ESC Y (row+VT52_COORD_BIAS) (col+VT52_COORD_BIAS)`. (Source: Edge Case Hunter.)
  - **Resolved by Story 1.11 (2026-05-11):** `render_emit_goto` in `src/render.asm` clamps both `row` to `[0, SCREEN_ROWS-1]` and `col` to `[0, SCREEN_COLS-1]` before adding `VT52_COORD_BIAS`. AC14 of Story 1.11 pins the rule; every `ESC Y` emit site in `render.asm` routes through this helper, so no other emit path can compose an unclamped GOTO.

## Deferred from: code review of story-1.3 (2026-05-09)

- **`search_pattern` / `ex_buffer` length-byte convention has no compile-time consumer enforcement.** Buffers reserve `1 + N` bytes (1 length prefix + N payload), documented in comments only. A consumer that reads `SEARCH_PATTERN_BUFFER` bytes from `search_pattern` instead of `search_pattern + 1` reads the length byte as data with no build-time signal. Belongs in ex/search consumer stories (2.1, 3.1) — add a named offset symbol (`search_pattern_text EQU search_pattern + 1`) when those stories first read the buffer. (Source: Blind Hunter.)
- **Static state has no zero-initialization story.** CP/M `.com` files do not zero TPA; `mode_byte`, `gap_start`, `cursor_offset`, `buffer_dirty`, etc. start as whatever garbage the previous program left. The current `RET`-stub doesn't read state, so it's inert today, but Story 1.4+ that reads any byte before writing reads stale RAM. Story 1.12 (init/teardown) must zero the static block on entry. (Source: Blind Hunter.)
  - **Resolved by Story 1.12 (2026-05-13):** `init_cold_start` (`src/init.asm`) zeroes the entire `static_data_base..static_end` region via a 1-byte-seed LDIR at cold-start stage 1. The gap-buffer region (`GAP_BUFFER_BASE..GAP_BUFFER_BASE + GAP_BUFFER_MAX`) is deliberately NOT touched per SR2 ("bytes inside the gap are read-as-undefined"); `gapbuf_init` at stage 2 establishes the SR2 invariant separately. The yank-register region (`yank_buffer..yank_end`) is also NOT zero-init per SR6 ("yank holds last-yank content; boot residue on first paste is the documented sharp edge").
- **No alignment / page-crossing consideration for `shadow_buffer`.** A page-aligned `shadow_buffer` enables an `H = row + base_high` render fast-path; the current layout makes no attempt to pad. Whether the win justifies the slack belongs to the render pipeline story (1.11) where indexing strategy is decided. (Source: Blind Hunter.)
  - **Resolved by Story 1.11 (2026-05-11):** Decision is **no page alignment**. `shadow_buffer` stays in its Story-1.3 layout slot. `render_emit_one_row` / `render_emit_status_row` compute `shadow_buffer + row * SCREEN_COLS + col` via a short shift-and-add idiom (HL doubled four times for ×16, plus two more doublings and a DE-save-add for ×80) — sub-perceptible at frame rate. The fast-path would have forced up to 255 bytes of slack between `filename_buffer` and `shadow_buffer`, which overlaps the `GAP_BUFFER_BASE` math and crowds the `yank_end <= 0xD800` budget. Alignment fast-path is deferred to a future profiling-driven story (none anticipated for the MVP code-size and frame-rate envelope).
- **Mode-state protocol undocumented.** The semantic relationship between `pending_operator`, `pending_motion_prefix`, `visual_submode`, and `count_accumulator` is not pinned in code or comments — whether they're orthogonal axes, mutually exclusive states, or sub-states of a state machine requires reading multiple stories' worth of context. Document in mode-dispatch / command-parser stories (1.9, 1.10) where the protocol is first implemented. (Source: Blind Hunter.)
- **`inc/state.inc` has no `IFDEF`-style re-include guard.** sjasmplus's natural EQU single-assignment catches accidental double-include with a duplicate-symbol error, but a defensive guard convention across all headers would catch it earlier and produce a more diagnostic message. Project-wide convention question — defer to a future structural-cleanup pass. (Source: Blind Hunter + Edge Case Hunter.)
- **No per-section sentinels with ASSERTs catching a missed `static_off` advance.** A forgotten `static_off = static_off + N` line silently aliases two adjacent fields with no build-time error. Adding `small_state_end`, `word_state_end`, `buffer_block_end` sentinels with `ASSERT` checks on their offsets would catch this. Defer until the layout grows further or a near-miss occurs. (Source: Blind Hunter + Edge Case Hunter.)
- **No file-naming or comment convention to distinguish EQU-only vs positional `.inc` headers.** state.inc must INCLUDE post-`ORG` (uses `$`); equates.inc / vt52.inc / modes.inc are safe pre-`ORG`. The distinction is documented only in two scattered comments. A naming convention (e.g., `*.eq.inc` vs `*.pos.inc`) or an explicit Header attribute in the AR23 block would make the rule structural. Project-wide question. (Source: Blind Hunter.)

## Deferred from: code review of story-1.4 (2026-05-09)

- **`BIOS_TICK_ADDR` placeholder address overlaps the BIOS jump-table neighborhood.** `EQU 0xFA00` sits six bytes below `BIOS_CONIN EQU 0xFA06`; on a typical CP/M 2.2 BIOS (17 × 3-byte jumps = 51 bytes from base) the tick counter lands inside the jump-table region. Spec Task 1 mandates the architecture-listed placeholder values verbatim, so the value cannot be moved here. Story 1.12 (init/teardown, hardware bring-up) confirms real BIOS-jump-table addresses against MicroBeast docs/disassembly and must lay them out so the tick counter sits outside the jump-table. (Source: Blind Hunter + Edge Case Hunter.)
  - **Resolved by Story 1.12 (2026-05-13):** W1 confirmation against MicroBeast BIOS 1.6+ vendor header (`src/bios_1_7.inc`) found the architecture's earlier placeholders off by a full 4 KB AND with CONIN / CONIST slots swapped. Corrected in `inc/bios.inc`:
      - `BIOS_START` (new) = `0xEA00` (was-implicit `0xFA00`).
      - `BIOS_CONINST` = `BIOS_START + 0x06` = `0xEA06` (was `0xFA09`; symbol kept as `CONINST`, vendor name `CONIST`).
      - `BIOS_CONIN` = `BIOS_START + 0x09` = `0xEA09` (was `0xFA06`).
      - `BIOS_CONOUT` = `BIOS_START + 0x0C` = `0xEA0C` (was `0xFA0C`; IFNDEF guard preserved).

    The MicroBeast BIOS does NOT expose a free-running tick counter at any documented address — the architecture's assumption (line 1386) was wrong. Resolution: VIBE installs its own 60 Hz user ISR via `MBB_SET_USR_INT` (vendor entry at `0xFDC7`) that increments `input_tick_counter` (new 16-bit field in `inc/state.inc`). `src/init.asm`'s cold-start defensively uninstalls any pre-existing ISR before the LDIR fill, then installs `input_tick_isr` (10-byte routine in `src/input.asm`); `init_teardown` uninstalls before warm-boot per the vendor warning that user ISRs survive reboots but their TPA memory does not. `BIOS_TICK_ADDR` retired from `inc/bios.inc`; `tick_wait_one` reads `input_tick_counter` directly. 60 Hz vs the architecture's 50 Hz: `ESC_TIMEOUT_TICKS = 2` ≈ 33 ms, still inside NFR4's 20-40 ms target — no equate retune needed.

## Deferred from: code review of story-1.5 (2026-05-09)

(All three originally-deferred items were promoted to patches and applied during the same review session — no outstanding deferrals from this story.)

## Deferred from: code review of story-1.6 (2026-05-09)

- **Multi-line iz-cpm stderr renders unreadably in the per-case fail echo.** `out=$(timeout … 2>&1)` folds stderr in; `echo "  fail $$case  (rc=$$rc, output: $$out)"` collapses newlines via shell IFS but leaks ANSI/control bytes from iz-cpm's `adm3a` terminal emulation. Cosmetic but real; consider per-case `.log` files in a future polish pass. (Source: Blind Hunter + Edge Case Hunter.)
- **No compile-time guard that `TEST_RESULT (0xCFFE)` is below the runtime BDOS base.** Prologue silently assumes the TPA extends through 0xCFFF. Adding an `ASSERT TEST_RESULT < BDOS_BASE` would couple the scaffold to a production-header symbol and break the deliberate scaffold/production decoupling per AC2. Revisit when the harness is ported to real hardware (Story 1.12). (Source: Blind Hunter.)
- **`clean` target removes only `.com` files.** Today sjasmplus emits only `.com` with the harness flag set; if a future story adds `--lst` / `--sld` / per-case symbol output, `make -C test clean` will leave artifacts behind. Expand the recipe (`rm -f cases/*.com cases/*.lst cases/*.sld`) when needed. (Source: Blind Hunter + Edge Case Hunter.)
- **`for com in $(COMS)` breaks on filenames with whitespace or shell metacharacters.** TH2 naming convention forbids these characters; enforce explicitly in a later lint pass if a near-miss occurs. (Source: Blind Hunter + Edge Case Hunter.)
- **Stale `cases/*.com` could survive a clock-skew rebuild.** Obscure: NFS clock drift or `touch -d` games could fool Make's timestamp check, leaving a previous-run's `.com` matched against a newly-edited `.asm`. Address only if seen in practice. (Source: Edge Case Hunter.)
- **`iz-cpm` not on PATH masquerades as universal test failure (rc=127, "command not found" folded into `$out`, no PASS/FAIL match → every case reports `unknown`).** Operational; not a Story 1.6 regression. Add a preflight `command -v iz-cpm` check in a future polish pass. (Source: Edge Case Hunter.)
- **Missing `timeout(1)` on macOS/BSD masquerades as universal test failure.** Same operational class as the iz-cpm preflight. README documents the limitation; defer the build-time guard. (Source: Edge Case Hunter.)
- **Sentinel addresses 0xCFFE/CFFF could land in the CP/M stack region.** Prologue does not set SP. Today iz-cpm's CCP-default SP is safely below 0xCFFE; revisit when porting the harness to real hardware in Story 1.12 (the architecture's stack base may differ). (Source: Edge Case Hunter.)
- **Sentinel could collide with the future production memory map** if a later test INCLUDEs `inc/state.inc` and the production layout grows to occupy 0xCFFE/CFFF. Static check belongs to whichever story first crosses that line. (Source: Edge Case Hunter.)
- **`cases/*.asm` glob picks up Emacs lock symlinks (`.#foo.asm`).** Make's wildcard matches them; sjasmplus then chokes on a symlink to nothing. Rare in practice; switch to `find` if it becomes a problem. (Source: Edge Case Hunter.)
- **Missing `cases/` or `inc/` directory yields a cryptic Make error.** Not a regression introduced by this story; operational concern only. (Source: Edge Case Hunter.)
- **`test/Makefile` does not duplicate the production `check-toolchain` step.** Spec line 461 explicitly accepts this — recursion from the project-root `Makefile` is the assumed entry point. Separate concern: whether the project-root `test:` target itself depends on `check-toolchain` (verify in a follow-up if NFR18 drift is observed). (Source: Edge Case Hunter.)
- **A wild jump into `test_fail` from a buggy body emits `FAIL <stack-garbage>` indistinguishable from deliberate fail.** Speculative; would need a "crashed" classification path with its own sentinel value. (Source: Edge Case Hunter.)
- **sjasmplus dot-prefixed local labels could leak between body and epilogue.** Theoretical: a body using `.x:` while a body-defined non-local label is the most-recent scope-anchor would put `.x` in the wrong scope. Document if a near-miss occurs; not seen in the demo cases. (Source: Edge Case Hunter.)
- **Fixture `hello.txt` lacks the CP/M `0x1A` EOF marker.** README claims CR+LF (which the file does have); CP/M canonical text fixtures additionally end in `^Z`. Surfaces in Story 2.2's fileio tests; not a Story 1.6 obligation. (Source: Edge Case Hunter.)
- **Hex-print helpers in `test_epilogue.inc` add ~30 unused bytes to every test case's `.com`.** NFR9 explicitly excludes test artifacts from the production code-size budget; gate behind `IFDEF` if a later story (e.g. fileio with large fixtures) needs the byte budget. (Source: Acceptance Auditor.)

## Deferred from: code review of story-1.7 (2026-05-10)

- **`gapbuf_move_gap` has no debug-build runtime check for out-of-range targets or `gap_size == 0` precondition.** Story dev-notes (Task 5) weighed "ASSERT in debug build" vs "document as caller-responsibility" and chose document-only. sjasmplus `ASSERT` is build-time only (operates on assembly-time constants); a runtime check requires (1) a project-wide `IFDEF DEBUG` build mode and (2) a chosen failure surface (`status_set_message`? halt? trap byte?). Neither exists today. The contract holds for internal callers (insert/delete pass `(cursor_offset)`, always valid by SR2). A misuse from a future story (motions, edits) would silently scribble past the gap on right-shift or read garbage on left-shift. Revisit when a project-wide `ASSERT_DEBUG` macro and debug-build mode land. (Source: Blind Hunter + Edge Case Hunter.)

## Deferred from: code review of story-1.8 (2026-05-10)

- **`input_held_flag` / `input_held_byte` are uninitialised at boot.** First-ever `input_get_key` call returns garbage if the RAM at the EQU'd addresses is non-zero on warm boot — the queue branch fires and returns `(input_held_byte)` without ever polling `BIOS_CONIN`. Static block has no zero-init in Story 1.8; spec explicitly defers init to Story 1.12 (matching the prior Story 1.3 deferral "Static state has no zero-initialization story"). `input_get_key` becomes callable in Story 1.9 (dispatch) and exercisable via the optional AC14 smoke, but no UAT is possible anyway until end of epic 1, so the gap closes naturally when Story 1.12's centralized init lands and zeros the whole static block. (Source: Blind Hunter + Edge Case Hunter.)
  - **Resolved by Story 1.12 (2026-05-13):** Centralised LDIR fill at `init_cold_start` stage 1 zeroes the whole static block, including `input_held_byte` and `input_held_flag`. First-ever `input_get_key` call now reads `input_held_flag == 0` and falls through to `BIOS_CONIN` as the queue-bypass contract intends. The new `test/cases/init_cold_start-state-shape.asm` headless test pins this post-init invariant (subtest 9, sentinel `0xE9`).
- **Queued-Esc bypasses Esc disambiguation (Esc-Esc-Arrow loses arrow synthesis).** When the user types `Esc Esc <arrow-suffix>`, the second Esc gets unrecognised by `synthesize_arrow_key` and stashed in `input_held_byte`; the next `input_get_key` returns it via the held-byte fast path which `RET`s without ever entering `.no_held`'s Esc-comparison branch. The arrow letter on the call after that reads as a plain ASCII byte. Real-keyboard double-Esc is rare in vi and is typically the user's deliberate "assert normal mode" gesture (not an Esc-followed-by-arrow). Fixing requires re-entering Esc disambiguation from the queue path — a meaningful redesign. Revisit only if hardware UAT shows it surfacing as a UX problem. (Source: Blind Hunter + Edge Case Hunter.)
- **`tick_wait_one` hangs forever if the BIOS ISR isn't ticking the counter.** `BIOS_TICK_ADDR` is a W1 placeholder (`0xFA00`, per `inc/bios.inc:65`). If the real MicroBeast BIOS jump-table layout puts the tick counter elsewhere or the ISR isn't armed at startup, a bare-Esc keypress spins forever in `tick_wait_one`'s `JR Z, .spin` with no escape — editor appears frozen. Documented as a Story 1.12 hardware bring-up concern; not the input layer's responsibility. (Source: Edge Case Hunter.)
- **`tick_wait_one` issues unconditional `EI`, clobbering caller's interrupt-disable state.** Today nothing calls `input_get_key` (or `tick_wait_one` directly) with `DI` active. Story 1.12 init is the natural first context where this could matter (BIOS bring-up may run with interrupts disabled). The clean fix is the standard IFF-preservation pattern (`LD A, I; JP PE/PO; PUSH AF; DI; ...; POP AF; ret-with-IFF-restore`) folded into a project-wide convention when 1.12 lands. The current contract is silent about IFF; widen it then. (Source: Blind Hunter + Edge Case Hunter.)
  - **Evaluated and still deferred — Story 1.12 (2026-05-13):** CP/M warm-boot hands the .com control with interrupts enabled (the BIOS keyboard / timer ISRs are running). `init_cold_start` brackets stages 0–2 in `DI` / `EI` to close the stale-user-ISR race window, but leaves interrupts enabled for the rest of the boot chain and for the input loop. `tick_wait_one`'s callers therefore reach it with IFF=1 (the CP/M warm-boot convention); the `DI / read / EI` bracket leaves IFF=1 on exit — matching the entry state for every current caller. Benign in Story 1.12's context, but note that the routine FORCES IFF=1 on exit rather than restoring the caller's IFF state; the rationale ("leaves IFF as it found it") only holds while every caller enters with IFF=1. Re-open when a future story legitimately enters `tick_wait_one` with IFF=0 (e.g. an ISR-driven story that wraps init for some hardware bring-up sequence); the proper IFF-preservation pattern (`LD A, I` / `JP PE` / `PUSH AF` / `DI` / read / `POP AF` / conditional-`EI`) lands then.

## Deferred from: code review of story-1.11 (2026-05-11)

- **TAB / CR / NUL / high-bit bytes render raw, desyncing shadow vs physical screen.** `render_emit_one_row` / `render_emit_status_row` emit `byte_at_logical`'s return value verbatim. TAB (0x09) advances the VT52 cursor 8 cells but shadow stores 0x09 at one cell — cells past the TAB never reconcile on subsequent frames. CR (0x0D) resets cursor to column 0 mid-row, causing later cell emits in the same row to overwrite cells 0..N. NUL / 0x80+ are terminal-dependent. Out of scope for Story 1.11 (no AC mandates control-char handling). Needs a design call (printable-only filter at emit, vi-style ^X notation, or insertion-time canonicalization) — surface during input-text-handling stories. (Source: Edge Case Hunter.)
- **Sloppy `top_line_offset` (not at line-start) is silently honored when cursor is in-range.** `render_scroll_adjust .row_in_range` trusts the caller's `top_line_offset`. A Story-1.7 paste or a future-story bug that leaves `top_line_offset` mid-line would render the visible window from mid-line with no recovery. Defensive hardening (idempotent `render_find_line_start` on the in-range path) was not part of AC4. (Source: Edge Case Hunter.)
- **`render_emit_goto` does not save D/E around `CALL render_emit_byte`.** `render_emit_byte`'s contract preserves D/E (holds today), but the routine's header says "BIOS may trash more". Latent until Story 1.12 hardware smoke confirms real BIOS_CONOUT register behavior. If BIOS trashes D or E, the second/fourth emit reads corrupted coordinates. Defensive fix is to hold biased row/col in module-local scratch (like the existing `render_cursor_row`/`_col`) instead of D/E. (Source: Edge Case Hunter.)
  - **Resolved by Story 1.12 (2026-05-13):** Story 1.12 hardware UAT surfaced exactly this regression. Initial symptoms on real MicroBeast: cursor jumping to random screen positions; status-banner fragments (`-`, `--`) scattered across the bottom row; mode banners never coherent. Diagnosis: every `ESC Y` sequence emitted by `render_emit_goto` was reaching the terminal with garbled row/col bytes — the MicroBeast BIOS_CONOUT trashes D or E (or both) across the call, so the second / fourth `LD A, D` / `LD A, E` read corrupted values. Patch: biased row/col now live in two file-local scratch cells (`render_goto_row` / `render_goto_col`) across the four emit_byte calls; the BIOS may trash any register at will. Cost: +10 bytes. Confirmed on hardware: status banners render coherently; cursor reposition lands at the correct row/col; sustained typing leaves no debris.
- **`render_in_run` not reset to 0 at end of `render_emit_status_row` when last cell differs.** Currently safe because every row's emit re-initializes `render_in_run` at entry. Fragile against a future standalone caller of `render_emit_status_row` that doesn't reset first; a one-instruction `XOR A / LD (render_in_run), A` before `RET` would close the seam. (Source: Blind Hunter.)
- **Per-cell shadow_ptr reload in the hot row loop.** `LD HL, (render_shadow_ptr) / ADD HL, DE` inside `render_emit_one_row`'s cell loop runs 80× per row × 23 rows ≈ 1840 redundant 16-bit loads per dirty frame. NFR3 currently meets budget on the test cases, but converting to a single incremented pointer would buy ~6 ms/frame at 4 MHz. Optimization opportunity for a future profiling-driven pass. (Source: Blind Hunter.)
- **`test_capture_len` is a single byte; wraps silently at 256.** `test/inc/test_bios_conout_capture.inc` uses a 1-byte length counter and a 256-byte capture buffer. Current render tests stay well under 256 bytes; a future test that triggers a full-redraw frame (>256 emitted bytes) would silently wrap the capture and could mask the actual emit stream. Harden when a render test legitimately exceeds 256 bytes (e.g., grow to 16-bit length + 1024-byte buffer, or add an assert-on-overflow sentinel). (Source: Blind Hunter.)
- **Iterative `render_scroll_adjust` advance has O(N × 1840) worst-case scan when the cursor jumps far below visible.** The patch applied for the 8-bit row-counter wrap converts the advance branch to "count rows (capped at EDITABLE_ROWS) → if cap hit, advance top by 1 LF → retry". Each iteration's count walks up to ~1840 bytes (EDITABLE_ROWS LFs from current top). For 1-row scrolls (normal motion), this is one iteration ≈ 1.4 ms at 4 MHz — fine. For cursor jumping N rows below visible, total scan ≈ N × 1840 bytes — N=100 stutters at ~140 ms, N=1000 at ~1.4 s. Reachable in epic 2's G (go-to-end) / gg / line-number jumps on large files. Proper fix is a ring-buffer of the last EDITABLE_ROWS line-start positions during a single forward walk (one pass cursor−top, ~46 bytes of scratch). Re-architect when the first far-jump motion lands. (Source: code-review triage, 2026-05-11.)

## Deferred from: code review of story-1.10 (2026-05-10)

- **Mode transitions don't clear parser state.** `enter_insert_mode`, `enter_visual_mode`, `enter_command_mode`, `enter_normal_mode` (in `src/dispatch.asm`) leave `count_accumulator`, `pending_operator`, and `pending_motion_prefix` untouched. Because parser state is global (state.inc-resident), a sequence like `5 v Esc d` composes a stale count=5 with the new operator d — surprising relative to vi. Cross-module concern; the policy ("clear on mode change" vs "preserve across mode change") belongs to a dedicated mode-transition story or to Story 1.12 init. (Source: Edge Case Hunter.)
  - **Evaluated and deferred to Story 2.5+ (2026-05-13):** Story 1.12 considered clearing parser state on mode-change but parser state is invisible at the user level until motion handlers consume it; mode transitions in Epic 1 produce no user-visible bug from the stale-state interaction. Real h/j/k/l motions (Story 2.5) make the count semantics observable; pin the policy then.
- **Unbound key in NORMAL doesn't clear parser state.** `dispatch.asm`'s `unbound_normal` surfaces a status message and `RET`s — parser state stays. So `5 g x` (x unbound) leaves count=5 and prefix='g' pending; the next 'g' fires the gg-stub spuriously. The fix is either `CALL parser_clear` inside `unbound_normal` or an explicit policy decision to preserve. Defer to the same mode-transition policy story as above. (Source: Edge Case Hunter.)
  - **Evaluated and deferred to Story 2.5+ (2026-05-13):** Same policy bucket as the mode-transition entry above; same observable-effect rationale. Pin both rules together when motion handlers land.
- **Stubs tail-JP `parser_clear` before count/operator can be captured by future real handlers.** `parser_doubled_operator_stub` and `parser_gg_motion_stub` zero all three parser fields atomically — Story 2.6 (real gg) and Story 2.10 (real dd/yy/cc/<<<>>) will need to read `count_accumulator` and `pending_operator` BEFORE the clear (so `5dd` deletes 5 lines, not 1). Add a `TODO(Story 2.x)` contract comment to each stub when those stories begin so the dev sees the constraint at the patch site. (Source: Edge Case Hunter.)
- **`parser_dispatch` IX safety not pinned.** No production caller exists yet (only headless tests). A future motion handler that trashes IX, invoked from within `dispatch_key`'s frame (which uses IX as the entries base), would corrupt the binary search. Two ways out: (a) document the IX constraint in MC4 motion-handler contracts when Story 2.5+ lands, or (b) wrap `parser_dispatch`'s `.invoke` trampoline in `PUSH IX / POP IX`. Defer pending the first real motion handler. (Source: Edge Case Hunter.)
- **`dispatch_visual` doesn't bind digits / operators / 'g'.** VISUAL mode currently routes everything except Esc to `unbound_visual`. With parser state global, count entered in NORMAL persists into VISUAL and back. Story 3.3 (visual character mode) and FR33-FR38 own the policy: "VISUAL clears parser state on entry" vs "VISUAL extension reads the count" is an open design question. (Source: Edge Case Hunter.)

## Deferred from: code review of story-1-12-init-teardown-on-hardware-smoke-test (2026-05-13)

- **`MBB_SET_USR_INT` return value ignored at all 3 sites.** Vendor doc returns previous ISR address in HL on every call. On a misbehaving BIOS that fails install, our 60 Hz ISR never registers, `input_tick_counter` never increments, and the first bare-Esc spins `tick_wait_one` forever — editor frozen, NFR5 violated. Hardware UAT iterations 2 and 3 demonstrated reliable install/uninstall on the target board (re-installable across back-to-back `vibe` invocations). A defensive check needs a failure-policy decision (status-banner an error and refuse the input loop? install retry? hang acceptable?) which is bigger than this story's scope. Re-open if a future BIOS revision changes the install semantics or if any UAT exhibits a non-incrementing counter. (Source: Edge Case Hunter, code-review triage 2026-05-13.)
- **`count_accumulator` unbounded growth first exposed by Story 1.12 input-loop wiring.** Story 1.12 is the first production caller of `parser_handle_digit` (via dispatch_normal's `0`..`9` entries). With no motion consumers in Epic 1, digits accumulate without user-visible effect (and the parser's overflow behavior — saturate or wrap — bites on the 6th 9). Sister-finding to the already-deferred "mode transitions don't clear parser state" and "unbound key in NORMAL doesn't clear parser state" entries from the Story-1.10 review; same Story-2.5+ landing zone when motion handlers make count semantics observable. (Source: Edge Case Hunter, code-review triage 2026-05-13.)
- **No `LD SP, ...` in `init_cold_start`; editor runs on CCP's default stack.** The call chain `input_get_key → dispatch_key (4-iter binary search) → handler → status_set_message → render_diff → render_emit_one_row → render_byte_at_logical` is bounded but not measured; CCP typically pushes only the warm-boot vector plus its own frame. 30 s sustained-typing hardware UAT (AC14 step 7) and back-to-back `vibe` invocations (AC14 step 9) demonstrated no overflow on the target board. Adding a dedicated stack buffer touches state.inc (cross-module patch) and adds ~64–128 B; defer until a future story (likely Epic 3's visual mode with deeper transitive call chains, or fileio's BDOS read/write nesting) either observes a corruption or measures the depth. (Source: Blind Hunter, code-review triage 2026-05-13.)
