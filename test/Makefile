# ============================================================
# VIBE — test/Makefile (stub)
#
# Real headless test harness lands in Story 1.6 (iz-cpm + sentinel-
# byte protocol per TH1–TH3). Story 1.1's job is to ensure
# `make test` from the top-level doesn't error.
# ============================================================

.PHONY: all test

all: test

test:
	@echo "test harness not yet wired — see Story 1.6"
